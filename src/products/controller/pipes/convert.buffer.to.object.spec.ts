import { convertBufferToObject } from './convert.buffer.to.object';

describe('Convert Buffer To Object', () => {
  it('should be defined', () => {
    expect(convertBufferToObject).toBeDefined();
  });
});
